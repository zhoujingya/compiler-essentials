## Check all log history about specific word in LLVM

Below example will show all the commit history containing the word "Opencl".

```
git log -i --grep=Opencl --reverse
```

## Reference

> All reference must be latest in LLVM commits

* New target: Xtensa

* New language: HLSL
